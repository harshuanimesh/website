# configurable-fields

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build configurable-fields` to build the library.

## Running unit tests

Run `nx test configurable-fields` to execute the unit tests via [Jest](https://jestjs.io).
